{
  "hookPath": "/",
  "hookPort": 8088,
  "projects": [
    {
      "name": "your-project",
      "path": "absolute/path/to/your-project",
      "secret": "verysecret",
      "exec": ["git pull", "npm run build", "pm2 restart your-project"],
      "filter": "\\[skip backend\\]"
    }
  ]
}
